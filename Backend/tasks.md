Write a 1-page document (fundamentals.md) covering: 
(1) the responsibilities of a frontend, a backend, and a database in a full stack request/response cycle, using the concrete example of a user submitting a login form; 
(2) why FastAPI is built and how that differs from a template-rendering framework; 
(3) one paragraph on where a developer's daily work fits into an Agile sprint. 
 
Then write 10 standalone Python snippets in snippets.py, each 3-8 lines, demonstrating: list comprehension, dict comprehension, f-string formatting, *args and **kwargs in a function signature, unpacking a list into variables, a lambda passed to sorted() with a key argument, a ternary expression, a with-statement for file handling, a set operation (union or intersection), and a dictionary .get() with a default value. Each snippet must run without errors via 'python snippets.py' and print its output to the console.
 
 
Deliverable: fundamentals.md (1 page) + snippets.py (10 working, tested snippets with visible console output).
 
Note: Use AI to explain any Python feature that is unclear (e.g. 'explain **kwargs with an example'). Every snippet submitted must be typed and tested independently, not pasted from AI output.
Create a virtual environment (python -m venv venv) and activate it. Inside it, model a small real-world system — a library — using 4 classes: Library, Book, Member, and Loan. Book must have at least 3 attributes with type hints (title: str, isbn: str, available: bool). Member must inherit shared behavior from a base Person class (name: str, email: str) via inheritance. Library must have methods add_book(book: Book) -> None, borrow_book(isbn: str, member: Member) -> Loan, and return_book(isbn: str) -> None, every method signature fully type-hinted including the return type. Raise and handle at least 2 custom exceptions (e.g. BookNotAvailableError, BookNotFoundError) with your own exception classes inheriting from Exception, and demonstrate catching them with specific except blocks (not a bare except). Write requirements.txt listing any installed packages, even if empty.
 
Deliverable: library.py with 4 fully type-hinted classes (Library, Book, Member/Person, Loan), 2 custom exceptions demonstrated with specific except blocks, run inside an activated virtual environment.
Git init/add/commit/push/pull; branching & merging; pull requests; resolving merge conflicts manually; .gitignore; user stories & acceptance criteria; functional vs non-functional requirements
 
Create a GitHub repository named python-training-<name>. Create 3 branches: feature/product-list, feature/product-detail, feature/product-create. On each, add one placeholder file with a short description, commit using the format 'feat: <description>', and open a pull request into main for each. Merge feature/product-list normally. Before merging feature/product-detail, edit the same line in main and in the branch to force a genuine merge conflict, then resolve it by manually editing the file (not auto-resolving with ours/theirs), and complete the merge. 
 
Separately, given the user story 'As a shopper, I want to search for products by name or category so that I can find what I want to buy quickly,' write requirements-breakdown.md containing: at least 5 functional requirements, at least 3 negative/edge-case scenarios (empty search, no results, special characters), acceptance criteria in Given/When/Then format, and 3 specific clarifying questions for a product owner (e.g. case sensitivity, partial vs exact match, result limit).
 
Deliverable: GitHub repo link showing 3 branches, 1 merged PR, 1 resolved merge conflict with visible commit history, plus requirements-breakdown.md.
Salam, tasks for today
 
Scaffold a React project with Vite (npm create vite@latest product-catalog -- --template react). Build exactly 4 components under src/components/: Header.jsx (site title + a placeholder nav with 3 links, no routing logic yet); ProductCard.jsx (props: name, price, category, inStock — rendered in a styled card with a visibly distinct badge when inStock is false); Button.jsx (props: label, onClick, variant — calls onClick when clicked); ProductList.jsx (prop: products array — maps over it and renders one ProductCard per item using product.id as the React key). In App.jsx, render Header then ProductList with a hardcoded array of at least 6 products, mixing inStock true/false. The app must run with 'npm run dev' with zero console errors and zero React key warnings.
 
Deliverable: Running Vite app with the 4 named components rendering 6+ hardcoded products, zero console warnings.
 
 
Planning a component tree and data flow before coding; lifting state up; conditional rendering (ternary vs &&); case-insensitive partial-match filtering logic.
 
Before writing code, write out the component tree and data flow in plan.md for this requirement: 'A product listing page showing at least 8 hardcoded products with name, price, category, and an in-stock badge, with a text input above the list that filters the visible products by name as the user types, case-insensitive, partial match.' The plan must state which component owns the filter text state, how the filtered list is computed, and which components receive which props. Only after the plan is written, implement it in the Day 4 project. The in-stock/out-of-stock badge must use a single conditional expression (ternary or &&), not duplicated JSX blocks.
 
Deliverable: plan.md + a working, filterable product listing page.
Install fastapi and uvicorn (pip install fastapi uvicorn --break-system-packages if needed). Create a project with this structure: app/main.py (creates the FastAPI() instance and includes routers), app/routers/products.py (an APIRouter for product endpoints), app/schemas/product.py (Pydantic models, built on Day 3), app/services/ (business logic, empty for now). In app/routers/products.py, implement a temporary in-memory list of 5 product dicts and build GET /products (list) and GET /products/{product_id} (single item, raising HTTPException(status_code=404) if not found). Run the app with 'uvicorn app.main:app --reload' and open http://localhost:8000/docs to confirm the Swagger UI correctly reflects both endpoints, including their response shapes. Note in a short comment at the top of main.py that this stack does NOT use Jinja2Templates or the Request object for HTML rendering — React owns the entire UI, and FastAPI here is API-only.
Tables, keys, and normalization; SELECT/JOIN/GROUP BY/aggregate queries; SQLAlchemy vs a raw driver (mysql-connector-python/PyMySQL) vs pandas read_sql; tradeoffs between ORM-style, raw SQL, and dataframe-based database access.
 
Create a MySQL database named python_training. Create exactly 4 tables: users (id INT PK AUTO_INCREMENT, name VARCHAR(100), email VARCHAR(100) UNIQUE, password_hash VARCHAR(255), role VARCHAR(20) DEFAULT 'user', created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP); products (id INT PK AUTO_INCREMENT, name VARCHAR(150), price DECIMAL(10,2), category VARCHAR(50), in_stock BOOLEAN DEFAULT true); orders (id INT PK AUTO_INCREMENT, user_id INT, status VARCHAR(20), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (user_id) REFERENCES users(id)); order_items (id INT PK AUTO_INCREMENT, order_id INT, product_id INT, quantity INT, FOREIGN KEY (order_id) REFERENCES orders(id), FOREIGN KEY (product_id) REFERENCES products(id)). Insert at least 5 rows into users, 10 into products, 5 into orders, 10 into order_items. Write the same 10 SQL queries TWICE, using two different Python approaches from this list: (a) SQLAlchemy Core or ORM, (b) a raw driver such as mysql-connector-python or PyMySQL, (c) pandas with read_sql. The 10 queries: active products only; products in one category ordered by price descending; a user by email; orders per user (GROUP BY + COUNT); top 3 highest-value orders (JOIN + SUM); order_items for one order with product names (JOIN); users who never ordered (LEFT JOIN + WHERE NULL); products never ordered; update a product's price; delete an order_item. Write a short comparison (5-8 sentences) of what was noticed about each approach — readability, amount of boilerplate, and how each would scale to a larger project.
 
Deliverable: schema.sql, the 10 queries implemented via two different Python approaches (e.g. sqlalchemy_queries.py and pandas_queries.py), and a written comparison.
Pydantic BaseModel; response_model; ConfigDict(from_attributes=True); Depends() for shared logic such as a DB session; status_code per operation; HTTPException for error responses.
 
In app/schemas/product.py, define 3 Pydantic models: ProductBase (name: str, price: float, category: str, in_stock: bool = True), ProductCreate(ProductBase) (used for POST request bodies), and Product(ProductBase) (adds id: int, used as the response_model, with model_config = ConfigDict(from_attributes=True) so it can be built from a database row or object). In app/services/db.py, set up a database connection using whichever approach was preferred from Day 2 (SQLAlchemy engine/session is the more common choice for FastAPI; document the choice in a comment). Rewrite the products router to implement all 5 CRUD endpoints against the real MySQL products table: GET /products (response_model=list[Product]), GET /products/{product_id} (response_model=Product, 404 if not found), POST /products (request body: ProductCreate, response_model=Product, status_code=201), PUT /products/{product_id} (request body: ProductCreate, response_model=Product, 404 if not found), DELETE /products/{product_id} (204 on success, 404 if not found). Use a FastAPI Depends() function get_db() that yields a database session/connection per request and closes it afterward. Test all 5 endpoints via the Swagger UI or Postman against real MySQL data.
 
Dliverable: Working FastAPI CRUD API for products backed by MySQL, using Pydantic request/response models and a Depends()-based database session.
Field() constraints (min_length/max_length/gt); using a Python Enum as a Pydantic field type; @field_validator custom rules; FastAPI's automatic 422 responses; custom exception handlers via @app.exception_handler; marshmallow/cerberus as alternative validation libraries worth knowing about.
 
Add validation constraints directly to the ProductCreate Pydantic model using Field(): name: str = Field(min_length=2, max_length=150); price: float = Field(gt=0); category must be restricted to exactly 'Electronics', 'Apparel', 'Home', 'Sports', 'Other' — implement this using a Python Enum class (ProductCategory) as the field's type, not a plain string with a manual check. Add one custom validator using @field_validator (Pydantic v2) that rejects the literal product name 'test' (case-insensitive) as a reserved value. Confirm that FastAPI automatically returns a 422 response with a detailed error body when these rules are violated — do not write manual validation code for rules Pydantic already enforces. Add one custom exception handler in main.py using @app.exception_handler(SomeCustomException) for at least one applicvation-specific error case (e.g. a custom DuplicateProductError raised when the same name+category combination already exists), returning a consistent {"error": <message>} JSON body. Separately, open the documentation for marshmallow or cerberus and write 3-4 sentences comparing either one to Pydantic, specifically on why Pydantic is the natural fit inside FastAPI. Build a Postman collection with exactly 15 requests: the 5 happy-path requests plus 10 negative cases — missing name, missing price, price=-5, price='abc', category='Toys' (not a valid enum value), name='A' (too short), name='test' (rejected by the custom validator), a non-existent id (GET and DELETE), a malformed JSON body, and a request with no Content-Type header. Export the collection as a .json file.
 
Deliverable: Updated ProductCreate schema with Field constraints, an Enum-based category field, one custom @field_validator, one custom exception handler, the marshmallow/cerberus comparison paragraph, and an exported Postman collection (.json) with all 15 requests and visible pass/fail results.
Applying the FastAPI + Pydantic + MySQL pattern to a new resource from memory; router/schema separation; Enum-typed status fields; re-using validation patterns.
 
Independently repeat the Week 2 pattern for a new resource, 'tasks' (fields: id, title, description, status — an Enum of 'todo'/'in_progress'/'done', due_date: date, assigned_to as an FK to users.id). Design and create the MySQL table with the CREATE TABLE statement; define TaskCreate and Task Pydantic schemas with the same style of Field constraints and Enum usage as Day 4; build all 5 REST endpoints connected to MySQL using the same database access approach chosen on Day 2/3; organize the code into app/routers/tasks.py and app/schemas/task.py, not one file. Build a Postman collection with at least 12 requests covering happy path and negative cases, following the same negative-case pattern used on Day 4. Build this as a fresh implementation applying the same patterns from memory, not by copying the products files line by line.
 
Deliverablr: app/routers/tasks.py, app/schemas/task.py, the tasks MySQL table, and a Postman collection (.json) with 12+ requests, submitted for mentor review.
Connect the Week 1 React project to the live Week 2 FastAPI backend (http://localhost:8000/products) instead of hardcoded data. Build ProductListPage.jsx that: (1) uses useEffect to fetch the product list on mount, storing it via useState alongside a loading boolean and an error string; (2) uses useMemo to compute a derived, sorted-by-price array, recalculated only when the raw list or a sortOrder state value changes — add a button toggling sortOrder between 'asc' and 'desc'; (3) uses useCallback to wrap an onCardClick handler passed to every ProductCard, and verifies with React DevTools (or a render counter inside ProductCard) that cards do not re-render unnecessarily when unrelated state changes; (4) uses useRef to hold a reference to a search <input> and auto-focus it on mount; (5) extracts the fetch-and-loading logic into a custom hook useProducts() in src/hooks/useProducts.js returning {products, loading, error}, used by ProductListPage instead of inlining the fetch. The page must correctly render the loading state, the error state (test by stopping the backend), and the final data state.

Deliverable:ProductListPage.jsx correctly using useState/useEffect/useMemo/useCallback/useRef, plus src/hooks/useProducts.js, with loading/error/data states all demonstrated.
Redux Toolkit configureStore; createSlice; createAsyncThunk; useSelector; useDispatch; the Provider component; structuring a store into single-responsibility slices; Context API vs Redux tradeoffs.
 
Install @reduxjs/toolkit and react-redux. Create src/store/index.js with configureStore. Create productsSlice.js (state: {items:[], status:'idle', error:null}) with a createAsyncThunk fetchProducts() calling the Week 2 GET /products endpoint, and cartSlice.js (state: {items:[]}, each item {productId, name, price, quantity}) with 3 reducers: addToCart, removeFromCart, updateQuantity. Wrap the app in <Provider store={store}>. Replace the Day 1 useProducts-based fetch in ProductListPage with useSelector/useDispatch reading from productsSlice, dispatching fetchProducts() on mount. Build CartSummary.jsx reading the cart via useSelector and displaying item count and total price (computed with useMemo). Add an 'Add to Cart' button on each ProductCard dispatching addToCart. Verify: the cart persists in the store while navigating within the session, and adding the same product twice increases its quantity rather than duplicating the cart entry.
 
Use AI to check the createSlice/createAsyncThunk boilerplate syntax if unsure, but write the reducer logic (addToCart quantity merge logic in particular) personally and test it against the 2 listed behaviors.
 
Delivery: store/index.js, productsSlice.js, cartSlice.js, CartSummary.jsx, and ProductListPage reading from Redux, verified against both listed behaviors.
Controlled inputs; client-side validation mirroring backend rules; dispatching thunks on submit; disabling submit while a request is pending; parsing and displaying Pydantic's server-side (422) validation errors per field.
 
Build CreateProductForm.jsx with controlled inputs for name, price, category (a <select> using the same 5 fixed enum values as the backend: Electronics, Apparel, Home, Sports, Other), and inStock (checkbox). Add client-side validation matching the backend rules exactly: name required 2-150 chars, price required and > 0, category must be selected, name cannot equal 'test' (case-insensitive). Show an inline error message directly under each invalid field, not a single generic banner. On submit, dispatch a new createProduct createAsyncThunk (added to productsSlice.js) that POSTs to the Week 2 API; the submit button must read 'Saving...' and be disabled while the thunk is pending, and the products list in the store must update automatically after a successful create without a manual page refresh. If the API returns a 422 with Pydantic's field error detail (from the Week 2 Day 4 validation), parse and display those server-side errors under the corresponding fields as well, alongside the client-side ones.
 
Deliverable: CreateProductForm.jsx wired to Redux, showing both client-side and parsed Pydantic server-side validation errors per field, with a working end-to-end create updating MySQL and the UI.
 
Deadline today
react-router-dom route setup; useParams; useNavigate; Link vs programmatic navigation; protected/nested route basics; edit and delete confirmation patterns.
 
Install react-router-dom. Set up 3 routes in App.jsx: '/' -> ProductListPage, '/products/:id' -> ProductDetailPage, '/products/new' -> CreateProductForm. Build ProductDetailPage.jsx reading the id via useParams, selecting the matching product from the store via useSelector (fetching the full list first via fetchProducts if the store is empty, e.g. on a hard refresh), and displaying all fields. Add an 'Edit' button showing an inline edit form (reusing the CreateProductForm field set and validation) pre-filled with current values, dispatching an updateProduct thunk on submit. Add a 'Delete' button showing a confirmation prompt before dispatching a deleteProduct thunk; after a successful delete, use useNavigate to redirect to '/'. Add a nav bar with Link components to '/' and '/products/new'.
 
Deliverable: 3 working routes with a useParams-driven detail page, useNavigate-driven redirect after delete, and full edit/delete wired to Redux and the MySQL-backed FastAPI.
 Applying the Redux + hooks + routing pattern to a new resource; component and slice reuse; explaining hook choices unprompted.
 
Independently build the full frontend for the Week 2 Day 5 'tasks' resource: tasksSlice.js (list/status/error state plus fetchTasks, createTask, updateTask, deleteTask thunks), TaskListPage.jsx, TaskDetailPage.jsx (route '/tasks/:id'), and CreateTaskForm.jsx — following the exact structural pattern used for products across Days 1-4 (Redux slice, useSelector/useDispatch, useParams/useNavigate, client + server validation display). Use useMemo at least once (e.g. filtering tasks by status) and useCallback at least once, and be able to explain, unprompted, why each was used. Adapt file names, component names, and field names correctly for tasks — this must not be a verbatim copy of the products files.
 
Deliverable: Full Tasks CRUD frontend (list/detail/create/edit/delete) connected to the Week 2 Tasks API via Redux, submitted for mentor review with a live walkthrough.
passlib CryptContext password hashing; python-jose/pyjwt signing & verification; FastAPI's OAuth2PasswordBearer; a get_current_user dependency; a require_role dependency factory; 401 vs 403; role-based UI restrictions on the frontend.
 
Add a role column (VARCHAR(20) DEFAULT 'user') to users if not already present. Install passlib (with the bcrypt extra) and python-jose (or pyjwt). Build POST /auth/register (hashes the password with passlib's CryptContext before storing; role defaults to 'user') and POST /auth/login (verifies the password, and on success signs a JWT containing {sub: email, role} with a 1-hour expiry). Build a get_current_user dependency using FastAPI's OAuth2PasswordBearer that decodes and verifies the JWT from the Authorization header, raising HTTPException(401) if missing/invalid/expired. Build a require_role(*allowed_roles) dependency factory raising HTTPException(403) if the current user's role is not in the allowed list. Apply exactly these rules to the products endpoints: GET requires get_current_user only (any role); POST/PUT require get_current_user + require_role('manager', 'admin'); DELETE requires get_current_user + require_role('admin'). Manually create one user of each role (user, manager, admin) in MySQL for testing. On the frontend, build LoginPage.jsx calling the login endpoint, storing the token and role in a new authSlice.js and in localStorage, and attaching the token as an Authorization header on every subsequent API call (via an axios instance interceptor or a fetch wrapper). Hide the 'Delete' button on ProductDetailPage unless the logged-in role is admin, and hide the 'Create Product' nav link unless the role is manager or admin.
 
Use AI to explain JWT structure/expiry mechanics or FastAPI's OAuth2PasswordBearer flow if unclear, but write and personally test get_current_user and require_role — auth logic must never be shipped unread.
 
Deliverable: Working register/login endpoints, get_current_user + require_role dependencies enforcing the exact rules above, tested with all 3 roles via a 12-request Postman collection showing correct 200/401/403 outcomes per endpoint/role combination, plus a working frontend login flow with role-based UI restrictions.
Task 1:
pydantic-settings and BaseSettings; .env configuration; FastAPI's CORSMiddleware restricted to a specific origin; documenting a reproducible setup process; debugging integration failures methodically.
 
Install pydantic-settings and create app/config.py with a Settings(BaseSettings) class reading DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, JWT_SECRET, and PORT from a .env file. Create a top-level .env.example documenting every required variable for both the backend and the frontend (VITE_API_BASE_URL). Add FastAPI's CORSMiddleware configured to allow only the frontend's specific origin, not a wildcard. Write SETUP.md with numbered steps to run the entire application from a clean clone: create and activate a virtual environment, install backend dependencies, install frontend dependencies, create the database and run schema.sql, copy .env.example to .env in both projects with real values, start the backend with uvicorn, start the frontend with npm run dev, confirm the app loads with live MySQL data. Have a peer or the mentor follow SETUP.md on a separate machine or a fresh folder with zero help; log every point of confusion and fix the document until it can be followed with no clarifying questions.
 
Deliverable: SETUP.md verified by a second person following it end to end with zero clarifying questions, app/config.py using pydantic-settings, .env.example for both projects, CORS restricted to the specific frontend origin.
 
Task 2:
pytest and FastAPI's TestClient; testing success and failure paths, not just the happy path; React Testing Library; mocking the Redux store for component tests; what makes a test meaningful vs superficial.
 
Install pytest and use FastAPI's TestClient (from fastapi.testclient import TestClient). Write 5 tests in tests/test_products.py: GET /products returns 200 and a list; GET /products/{id} with a valid id returns 200 and the correct product; GET /products/{id} with an invalid id returns 404; POST /products without a valid auth token returns 401; POST /products with a valid manager token but a missing name returns 422. Install @testing-library/react and a test runner matching the Vite setup (vitest). Write 5 frontend tests: ProductCard renders the name and price passed as props; ProductCard shows an 'Out of Stock' badge only when inStock is false; CreateProductForm shows a validation error when submitted with an empty name; clicking the sort toggle on ProductListPage changes the displayed order (mock the Redux store with a small fixed list); ProductDetailPage shows a 'Product not found' message when the store has no product matching the URL id. All 10 tests must pass — pytest for the backend, npm test for the frontend.
 
10 passing automated tests (5 backend with pytest/TestClient, 5 frontend with Testing Library), with full test output captured in a text file or screenshot.
End-to-end feature ownership; combining RBAC, Pydantic validation, Redux, routing, and testing into one deliverable.  writing an honest status report.
 
Build a complete ticket management module end to end, treated as a real assigned feature: MySQL table tickets (id, title, description, status — an Enum of 'open'/'in_progress'/'resolved'/'closed', priority — an Enum of 'low'/'medium'/'high', created_by FK to users.id, assigned_to FK to users.id, created_at); a FastAPI REST API with all 5 CRUD endpoints, Pydantic schemas with Field constraints and Enum-typed status/priority, and RBAC applied so any authenticated user can view and create tickets, only the assigned user or an admin can change status, and only an admin can delete; a React frontend with a Redux ticketsSlice, a list page filterable by status using useMemo, a routed detail page, a create form with full validation, and role-based UI restrictions matching the backend rules; at least 6 automated tests (backend + frontend) covering the new endpoints/components. At the end of the day, submit a half-page written status report covering what was built, what is not yet done, known issues, and a recommendation on whether this module is ready to ship.
 
Deliverable:
Complete Tickets module (MySQL table + REST API + RBAC + React/Redux frontend + 6 tests) plus a written status report, submitted for final mentor review.           
Uvicorn/Gunicorn workers vs --reload mode; production builds; secrets management; disabling interactive docs in production if required; CORS in production; health-check endpoints; hosted MySQL considerations (SSL, IP allowlisting).
 
Run the frontend production build and confirm a dist/ folder is produced with zero build errors. Write DEPLOYMENT.md covering: running the FastAPI app with Uvicorn workers (or Gunicorn managing Uvicorn workers) instead of --reload mode; moving all secrets (DB credentials, JWT_SECRET) out of any committed file into the hosting platform's environment variable settings; disabling FastAPI's interactive docs (/docs, /redoc) in production if required by policy; confirming CORS is restricted to the real production frontend domain, not localhost; and adding a GET /health endpoint returning {"status": "ok"} with 200 for uptime monitoring. Document specifically what changes moving from a local MySQL instance to a managed/hosted instance (connection string format, SSL requirement, IP allowlisting). Implement the /health endpoint.
 
Production build (dist/) artifact, working GET /health endpoint, and DEPLOYMENT.md covering all 5 checklist items with specifics, not generic statements.
